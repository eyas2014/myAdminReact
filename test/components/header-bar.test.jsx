import {shallow, mount} from 'enzyme';
import El from '../../src/components/header-bar/header-bar.jsx';
import HeaderMessages from '../../src/components/header-bar/header-messages/header-messages.jsx';
import HeaderNotifications from '../../src/components/header-bar/header-notifications/header-notifications.jsx';
import HeaderTasks from '../../src/components/header-bar/header-tasks/header-tasks.jsx';



describe('reactApp', function(){
/*	it('should shallow-render correct number of tags', function(){
		const wrapper=shallow(<El />);		
		assert.equal(wrapper.find("div").length),8);
		assert.equal(wrapper.find("span").length),7);
		assert.equal(wrapper.find("li").length),5);
	});

	it('should full-render correct number of tags', function(){
		const wrapper=mount(<El />);		
		assert.equal(wrapper.find("div").length),35);
		assert.equal(wrapper.find("span").length),28);
		assert.equal(wrapper.find("li").length),31);
	});   

	it('header-messages should full-render correct number of tags', function(){
		const wrapper=mount(<HeaderMessages />);		
		assert.equal(wrapper.find("div").length),13);
		assert.equal(wrapper.find("span").length),6);
		assert.equal(wrapper.find("li").length),9);
	});   

	it('header-notifications should full-render correct number of tags', function(){
		const wrapper=mount(<HeaderNotifications />);		
		assert.equal(wrapper.find("div").length),3);
		assert.equal(wrapper.find("span").length),6);
		assert.equal(wrapper.find("li").length),9);
	});   

	it('header-tasks should full-render correct number of tags', function(){
		const wrapper=mount(<HeaderTasks />);		
		assert.equal(wrapper.find("div").length),11);
		assert.equal(wrapper.find("span").length),9);
		assert.equal(wrapper.find("li").length),8);
	});   
*/

})

